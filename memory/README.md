# Memory Folder

Use this folder for private daily/session memory after setup.

Files in `memory/*.md` are ignored by default. Keep this README public so the folder purpose is visible.

Suggested private files:

- `memory/YYYY-MM-DD.md`
- `memory/run-log.md`
- `memory/learning-proposals.md`
